@{
}
@model MrFixIt.Models.Job

<div class="job-form">

    <h2>Would you like to do this job?</h2>
    <hr />
    <form data-url-action='@Url.Action("Claim", "Jobs")' class="confirm-claim">
        <input type="hidden" value="@Model.JobId" />
        <input type="hidden" value="@Model.Title" />
        <input type="submit" value="Claim This Job" />
    </form>
    @*@using (Html.BeginForm(FormMethod.Post, new { @class = "this-class" }))
    {
        @Html.HiddenFor(model => model.JobId)
        @Html.HiddenFor(model => model.Title)

        <h4>@Model.Title</h4>
        <input type="submit" value="Claim This Job" />
    }*@
    <p>@Html.ActionLink("Back to list", "Index")</p>

</div>